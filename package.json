{
  "name": "skill-sharing",
  "private": true,
  "main": "src/index.js",
  "type": "module",
  "scripts": {
    "build": "npm run test && npm run check",
    "start": "node src/main.js",
    "test": "node --experimental-vm-modules node_modules/jest/bin/jest.js",
    "check": "prettier . --check && eslint src test",
    "format": "prettier . --write"
  },
  "dependencies": {
    "express": "4.18.2"
  },
  "devDependencies": {
    "eslint": "8.49.0",
    "jest": "29.7.0",
    "prettier": "3.0.3"
  }
}
