{
  "name": "skill-sharing",
  "version": "0.1.0",
  "type": "module",
  "workspaces": [
    "skill-sharing-shared",
    "skill-sharing-web",
    "skill-sharing-server"
  ],
  "scripts": {
    "build": "npm run dist --workspaces --if-present && npm run test && npm run check",
    "clean": "npm run clean --workspaces --if-present",
    "test": "node --no-warnings=ExperimentalWarning --experimental-vm-modules node_modules/jest/bin/jest.js",
    "check": "prettier . --check && eslint */src */tests",
    "format": "prettier . --write && eslint --fix */src */tests",
    "start": "npm run start --workspace=skill-sharing-server",
    "devserver": "npm run dev --workspace=skill-sharing-server",
    "devweb": "npm run dev --workspace=skill-sharing-web"
  },
  "devDependencies": {
    "@jest/globals": "29.7.0",
    "eslint": "8.50.0",
    "jest": "29.7.0",
    "prettier": "3.0.3"
  }
}
